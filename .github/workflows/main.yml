name: Pylint v2

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.9", "3.10"]
    steps:
    - uses: Silleellie/pylint-github-action@v2.1
      with:
        lint-path: .
        python-version: 3.11
